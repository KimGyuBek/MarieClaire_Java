server:
  port: 8081
  servlet:
    session:
      tracking-modes: cookie
#      timeout: 60

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/marieclaire
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace